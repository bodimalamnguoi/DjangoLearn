version: '3.7'

services: 
  web:
    build: .
    command: python /code/manage.py runserver 0.0.0.0:8000
    environment: 
      - SECRET_KEY=ff=gd#pj8s@!0j_6!jmq3axuv3#iy4lptni!=k)_jiftg%jv$$^
      - DEBUG=1
    volumes: 
      - .:/code
    ports: 
      - 8000:8000
    depends_on: 
      - db
  
  db:
    image: postgres:11
    environment: 
      POSGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes: 
      - postgres_data:/var/lib/postgresql/data/

volumes: 
  postgres_data:
